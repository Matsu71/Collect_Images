# Collect Images 作業方針

## 目的

現生生物の姿・主要な特徴が分かり、将来の商用サービスでも条件を守って再利用できる実写素材を、画像本体・出典・ライセンス・クレジットと一緒に収集する。ChronoEarthに閉じない独立した画像集とする。保存先は `Matsu71/Collect_Images`。元のChronoEarthのデータ・コード・公開サービスは、この収集作業では変更しない。

現在の1,000分類群を起点とし、主画像が未確保の分類群を優先する。将来の1,000〜2,000分類群への拡充に対応するが、対象数や画像数を増やすために品質基準を下げない。補足写真の数を主画像の確保数として数えない。画像を取得しただけ、収集用コードを作っただけでは完了ではない。

## 開始時に読むもの

`README.md`、`reports/curation-status.json`、`reports/primary-gaps.json`、最新の `reports/continuation-result-*.json`、関連する `batches/` の保存結果を確認する。取得済み画像を重複して集め直すのではなく、未確保理由と前回の除外理由から再開する。他者の変更を破棄せず、push前にリモートとの整合を確認する。force pushは行わない。

## 品質・分類の確認

- 主画像は、動物では頭部・胴体・主要な肢・尾等の形が分かり、植物では草姿・樹形・分枝などが分かる写真を優先する。全ての肢先や根まで写ることを一律には求めないが、部分写真を黙って全体像として採用しない。
- 花・葉・果実・頭部だけの接写、幼体、標本、殻、顕微鏡像、発光現象、培養コロニー、複合パネル等は用途と限界を注記する。標本を生体、模式図や3Dモデルを実写、宿主を病原体として扱わない。
- 作者・ライセンスの自動照合と、目視による写真の選定、専門的な種同定は別工程である。実際に見ていない写真を目視確認済みにしない。一覧での確認を全件の原寸・画素単位の確認と書かない。
- タイトル・説明・画像の対象が矛盾する場合は採用を保留する。同名検索で出たものを自動的に同種としない。別の対象へ対応付けを訂正する場合は、出典で明示された学名と既存の対象IDの一致、訂正理由、元の検索対象を保持する。

## 権利情報

写真自体に適用されるCC0・CC BY・CC BY-SAの条件を画像単位で確認する。NC、ND、All rights reserved、権利不明の素材を採用しない。サイト全体や観察データのライセンスを画像へ転用しない。作者名、作品名、出典ページ、元画像URL、ライセンスのバージョンとリンク、提供された帰属表記、取得日時、加工内容を保持する。

CC0で作者名が取得できない場合は明示し、権利表記を人名として扱わない。CC BY等で必要な帰属情報が不足する場合は保留する。権利元の申告を確認したことと、すべての第三者権利を保証することを混同しない。

## 保存・台帳

`images/`、`metadata/`、`reference/images/`、`reference/metadata/`は取得資料を含む。利用先の採用は `collection/primary.json`、`collection/assets.json`、`collection/taxa.json` を使う。生の保存フォルダを一括採用しない。

新しい収集単位は `batches/` に分け、候補の元メタデータとプレビュー、ファイルハッシュ、目視判定、除外理由を残す。確認に使った候補台帳を後から書き換えない。保存用写真と確認用プレビューが同じ元画像であることを照合する。既存画像とJPEG・WebP・サムネイルなどの別形式を追加写真として水増ししない。

## 検証・報告

審査済み台帳の生成は `python curation/run_reviewed_build.py`。個別JPEG確認、作者欄の正規化、追加候補のハッシュ付き目視判定を反映するため、内部の `build_collection.py` だけで最終化しない。

別サービス向けの書き出しは `tools/export_collection.py`、書き出し先のオフライン写真一覧は `tools/build_export_gallery.py` を使う。画像本体の保存、ハッシュとデコード、ライセンス名とURL、出典と作者、一覧のローカルリンク、書き出し結果を確認する。保存・目視判定・mainへのpush・利用先への反映を区別して報告する。

最終報告では、前回から増えた実画像数・主画像を確保できた分類群数・未確保数を、保存済み台帳から集計する。未完了や保留事項を隠さず、引き継げる記録を残す。ユーザーが明示していない定期実行や公開サービスへの自動反映は追加しない。
